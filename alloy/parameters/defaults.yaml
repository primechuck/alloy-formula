# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: alloy
  rootgroup: root
  config: '/etc/alloy'
  service:
    name: alloy
  subcomponent:
    config: '/etc/alloy-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
