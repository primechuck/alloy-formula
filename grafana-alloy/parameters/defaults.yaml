# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: grafana-alloy
  rootgroup: root
  config: '/etc/grafana-alloy'
  service:
    name: grafana-alloy
  subcomponent:
    config: '/etc/grafana-alloy-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
